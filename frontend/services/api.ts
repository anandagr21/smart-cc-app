/**
 * Service: api
 * Responsibility: Base HTTP client configuration (Axios/Fetch).
 *
 * Architectural Boundaries:
 * - Centralized API layer.
 * - Handles base URLs, interceptors (e.g., auth tokens), and global error handling.
 *
 * TODO:
 * - Configure base HTTP client.
 */
