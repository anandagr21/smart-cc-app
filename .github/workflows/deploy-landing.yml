name: Deploy Landing Page to Cloudflare Pages

on:
  push:
    branches:
      - 'land_release_*'

env:
  CLOUDFLARE_ACCOUNT_ID: 952180b7ad12238ca1a7f1d9089e4b26
  PROJECT_NAME: smart-cc-landing

permissions:
  contents: read

jobs:
  deploy:
    name: Build & Deploy Landing Page
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: landing

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Dependencies
        run: npm ci

      - name: Build
        run: npx vite build

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ env.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy dist --project-name ${{ env.PROJECT_NAME }} --branch main --commit-dirty=true
          workingDirectory: landing
